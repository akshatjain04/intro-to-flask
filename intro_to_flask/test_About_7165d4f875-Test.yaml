# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p99: 200
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "test_About_7165d4f875-Test.csv"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/about"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "header"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid_url"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/about"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "header"

