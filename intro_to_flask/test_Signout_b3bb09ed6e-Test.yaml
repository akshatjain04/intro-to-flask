# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "test_Signout_b3bb09ed6e-Test.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/signout"
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                message: "Successfully signed out"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalidURL"
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - post:
          url: "/signout"
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                message: "Successfully signed out"
