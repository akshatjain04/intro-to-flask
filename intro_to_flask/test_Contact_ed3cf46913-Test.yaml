# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "test_Contact_ed3cf46913-Test.csv"
    fields: ["subject", "sender", "recipients", "name", "email", "message"]
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/contact"
          headers:
            Content-Type: "application/json"
          json:
            subject: "{{ subject }}"
            sender: "{{ sender }}"
            recipients: "{{ recipients }}"
            name: "{{ name }}"
            email: "{{ email }}"
            message: "{{ message }}"
          expect:
            - statusCode: 200
            - hasProperty: "success"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/wrong-url"
          headers:
            Content-Type: "application/json"
          json:
            subject: "{{ subject }}"
            sender: "{{ sender }}"
            recipients: "{{ recipients }}"
            name: "{{ name }}"
            email: "{{ email }}"
            message: "{{ message }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/contact"
          headers:
            Content-Type: "application/json"
          json:
            subject: "{{ subject }}"
            sender: "{{ sender }}"
            recipients: "{{ recipients }}"
            name: "{{ name }}"
            email: "{{ email }}"
            message: "{{ message }}"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "success"
