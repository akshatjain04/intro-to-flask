# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "test_Signup_9071453400-Test.csv"
    fields:
      - "firstname"
      - "lastname"
      - "email"
      - "password"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/signup"
          headers:
            Content-Type: "application/json"
          json:
            firstname: "{{ firstname }}"
            lastname: "{{ lastname }}"
            email: "{{ email }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            - hasProperty: "email"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/signup"
          headers:
            Content-Type: "application/json"
          json:
            firstname: "{{ firstname }}"
            lastname: "{{ lastname }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/signup"
          headers:
            Content-Type: "application/json"
          json:
            firstname: "{{ firstname }}"
            lastname: "{{ lastname }}"
            email: "{{ email }}"
            password: "verylongpasswordthatmightcausesomeissues"
          expect:
            - statusCode: 200
            - hasProperty: "email"
